These fixtures are output-quality calibration examples inspired by Autochrome-style structural diffs.

Goals for the rendered output in this round:
- keep unchanged structure visible
- keep wrapped or reshaped forms low-noise
- remain readable when nested forms move across lines

These fixtures are not production Clojure support. They exercise the fallback renderer only.
